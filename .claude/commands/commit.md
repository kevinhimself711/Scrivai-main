# Commit Command

自动生成 commit message 并提交代码。

## Usage

`/commit [message]`

## 执行流程

1. **状态检查**: 运行 `git status` 查看变更（禁用 `-uall` 标志）
2. **差异分析**: 运行 `git diff --staged` 和 `git diff` 查看具体变更
3. **风格参考**: 运行 `git log --oneline -10` 查看最近提交风格
4. **生成 Message**: 基于变更内容生成符合规范的 commit message
5. **执行提交**: 暂存文件并提交

## Commit Message 规范

### 格式
```
<type>: <description>

[optional body]
```

### Type 类型
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `refactor`: 重构（无功能变化）
- `test`: 测试相关
- `chore`: 构建/工具/配置
- `perf`: 性能优化
- `ci`: CI/CD 相关

### 规则
- Description **必须使用中文**
- 不超过 72 字符
- 使用祈使句（如 "添加用户认证" 而非 "添加了用户认证"）

## 安全规则

> [!CAUTION]
> **严禁**在 commit message 中添加任何 AI 标识:
> - 禁止 `Co-Authored-By: Claude`
> - 禁止 `🤖 Generated with...`
> - 禁止任何 AI 署名或标记

## 敏感文件检查

提交前检查以下文件是否被意外添加：
- `.env` / `.env.*`
- `credentials.json`
- `*_secret*`
- `*.pem` / `*.key`

如发现敏感文件，**警告用户**并等待确认。

## Arguments

$ARGUMENTS:
- `[message]` - 可选，直接指定 commit message（跳过自动生成）

## 示例

```bash
# 自动生成 message
/commit

# 指定 message
/commit "feat: 添加用户登录功能"
```
